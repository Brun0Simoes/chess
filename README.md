# AlphaZero Arena

Frontend GitHub Pages para jogar contra o backend Docker local da arena AlphaZero.

- O GitHub Pages hospeda apenas a interface.
- O servidor Docker executa os modelos e responde via HTTP/WebSocket.
- Configure a URL do backend no campo `URL do servidor Docker` ou use `?backend=https://sua-url`.
